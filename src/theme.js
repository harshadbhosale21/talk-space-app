import { createTheme } from "@mui/material/styles";

const theme = createTheme({
    palette: {
        primary: {
            main: "#c104de",
        },
        secondary: {
            main: '#383b38', // Secondary color
        },
    },
});

export default theme;
